<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <title>Edit Notification</title>
  </head>
  <body>
    <div class="form">
      <h1 class="fill">Edit Notification</h1>
        <%= link_to "", "/notifications/see_all_notify", class: "logout" %>
        <%= form_tag("/notifications/modify", :method=>"post") do %>
    <div class="field-wrap">
      <%= text_field_tag(:reminder, @notify_str.notify_string, :required=> true) %>
    </div>
    <div class="field-wrap">
    <div class="field_letter">
      Reminder Date<span class="req">*</span>
    </div>
    <div class="select_date_style">
      <%= select_date Date::today, :prefix => 'date_prefix' %>
    </div>
    </div>
      <%= submit_tag("SAVE", class: "button button-block") %>
      <% end %>
    </div>
    <%
    if session[ :emsg ]=="filled" %>
      <script>
        window.alert("reminders Updated successfully!");  
      </script> 
    <%
    session[ :emsg ]=nil
    end
    %>
  </body>
</html>